# 업데이트 요약 — 2026-02-18

## 1. Gmail 발신자 설정 동적화

**변경 동기**: TLDR AI 실제 발신자가 `@tldr.tech` 도메인이 아닌 `dan@tldrnewsletter.com`임을 확인.

**변경 내용**:
- `backend/app/config.py` — `newsletter_senders: list[str]` 필드 추가 (기본값: `["dan@tldrnewsletter.com"]`)
- `backend/app/services/gmail.py:69` — 하드코딩된 `from:@tldr.tech` 쿼리를 `settings.newsletter_senders` 기반 동적 쿼리로 변경
  ```python
  sender_parts = " OR ".join(f"from:{s}" for s in settings.newsletter_senders)
  query = f"({sender_parts}) after:{after_date}"
  ```
- `backend/.env` / `backend/.env.example` — `NEWSLETTER_SENDERS=["dan@tldrnewsletter.com"]` 추가

**발신자 추가 방법**: `.env`의 `NEWSLETTER_SENDERS` 값만 수정
```
NEWSLETTER_SENDERS=["dan@tldrnewsletter.com","@tldr.tech"]
```

---

## 2. 임베딩 모델 변경 및 배치 처리 적용

**변경 동기**: `gemini-embedding-001` 모델 적용 및 기사별 개별 API 호출 → 배치 호출로 효율화.

**변경 내용**:
- `backend/app/services/vector.py`
  - 모델: `text-embedding-004` → `gemini-embedding-001`
  - `_get_embedding(str)` → `_get_embeddings(list[str])`: 여러 텍스트를 한 번의 API 호출로 임베딩
  - `add_article_to_vector_db()` → `add_articles_to_vector_db(articles: list[dict])`: 배치 upsert
  - `search_similar()`: `_get_embeddings([query])[0]` 패턴으로 통일
- `backend/app/services/gemini.py`
  - `run_ai_processing()` 내 for 루프에서 `vector_batch` 수집 후 루프 완료 시 `add_articles_to_vector_db(vector_batch)` 1회 호출
  - AI 배치(5개) 처리당 임베딩 API 호출: 5회 → 1회

**주의**: 기존 ChromaDB 데이터와 차원 불일치 발생 → `chroma_db` 디렉토리 초기화 필요.

---

## 3. 동기화 기간 단축

- `backend/app/main.py`, `backend/app/routers/sync.py`
- `days=30` → `days=7` (최초 실행 및 `/sync` 엔드포인트 모두 적용)

---

## 4. 데이터 초기화

- `backend/tldr.db` 삭제
- `backend/chroma_db/` 디렉토리 삭제
- `backend/token.json`, `backend/credentials.json` 유지
